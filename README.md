# JS.WAT


This is an experimental programming language that compiles to WASM. It is designed to be able to very easily create bindings with support for WASI.
The language is a subset of JS syntax, similar to AssemblyScript.

